import * as React from "react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function DatasetManager() {
    return (
        <Card>
            <CardHeader><CardTitle>Dataset Manager</CardTitle></CardHeader>
            <CardContent>Dataset list and toggles coming soon.</CardContent>
        </Card>
    )
}
